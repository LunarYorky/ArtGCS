using HtmlAgilityPack;

if (args.Length < 1)
    return;

// var doc = new HtmlDocument();
// doc.LoadHtml(File.ReadAllText("RS.txt"));
//
// var node = doc.DocumentNode.SelectSingleNode(
//     ".//div[@id='columnpage']/div[@class='submission-content']/section");
